@inherits ListPageBase<SignaturePoolAdminDTO, SignaturePoolAdminSearchParamsDTO>
@attribute [Authorize(Policy = "AdminRoles")]
@inject IAdminService adminService
@page "/admin/signature-pools"

<_AuthorizationWrapper PageTitle="Signature pools list">
    <_PagedListWrapper PagedListDto="PagedListDTO" result="apiResponse">
        <BrowserAdminSignaturePools Refresh="Refresh" SearchParams="searchParamsDTO" TotalPages="PagedListDTO.TotalPages" TotalItems="PagedListDTO.TotalItems" />
        <HeaderAdminSignaturePools Refresh="Refresh" SearchParams="searchParamsDTO" />
        <_ForEachPagedListWrapper PagedListDto="PagedListDTO" result="apiResponse">
            <ReadSignaturePoolAdmin SignaturePool="@context" Refresh="Refresh" />
        </_ForEachPagedListWrapper>
    </_PagedListWrapper>
</_AuthorizationWrapper>

@code {
    protected override void InitializeSearchParams() => searchParamsDTO = new SignaturePoolAdminSearchParamsDTO();

    protected override async Task<ApiResponse<PagedListDto<SignaturePoolAdminDTO>>> Get(SignaturePoolAdminSearchParamsDTO searchParamsDTO)
        => await adminService.GetSignaturePoolsBySearchParams(searchParamsDTO);
}
