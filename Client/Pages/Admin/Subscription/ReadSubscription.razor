@inherits Read
@inject NavigationManager NavigationManager

<div class="card m-1 p-1">
    <div class="row mb-3">
        <div class="col-sm-2">
            <p class="form-control-plaintext" id="user-email">@SubscriptionDTO.ApplicationUserEmail</p>
        </div>
        <div class="col-sm-2">
            <p class="form-control-plaintext" id="begin-date">@SubscriptionDTO.Begin?.ToString("g", CultureInfo.InvariantCulture)</p>
        </div>
        <div class="col-sm-2">
            <p class="form-control-plaintext" id="end-date">@SubscriptionDTO.End?.ToString("g", CultureInfo.InvariantCulture)</p>
        </div>
        <div class="col-sm-3">
            <p class="form-control-plaintext" id="is-active">@SubscriptionDTO.IsActive</p>
        </div>
        <div class="col-sm-3">
            <div class="btn-group my-2" role="group">
                <a href="/admin/create-subscription/@SubscriptionDTO.ApplicationUserId?returnUrl=@Uri.EscapeDataString(NavigationManager.Uri)" class="btn btn-secondary btn-sm me-2">Edit</a>
                <button class="btn btn-primary btn-sm" type="button" @onclick="ToggleDetails">@ShowDetailsButtonText</button>
            </div>
        </div>
    </div>


    <div class="@DetailsCssClass">
        <hr />
        <h5>Subscription Details</h5>
        <dl class="row mb-3">
            <dt class="col-sm-3">ApplicationUserId</dt>
            <dd class="col-sm-9">@SubscriptionDTO.ApplicationUserId</dd>

            <dt class="col-sm-12">Payments:</dt>
            <dd class="col-sm-12">
                @foreach (var p in SubscriptionDTO.PaymentDTOs)
                {
                    <ReadPayment dto="p" Accept="AcceptPayment" />
                }
            </dd>
        </dl>
    </div>
</div>

@code {
    [Parameter]
    public SubscriptionDTO SubscriptionDTO { get; set; }
    [Parameter]
    public EventCallback<Guid> AcceptPayment { get; set; }
}
